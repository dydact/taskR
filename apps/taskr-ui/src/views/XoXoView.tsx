import React from "react";
import { PlaceholderView } from "./PlaceholderView";

export const XoXoView: React.FC = () => {
  return (
    <PlaceholderView
      title="xOxO Assignments"
      description="One-on-one dedicated AI agent assignments with real-time streaming updates. View agent status, prompt history, context, and Polaris obligations."
    />
  );
};

export default XoXoView;
